[tool.uv.workspace]
members = [
    "packages/api",
    "packages/mcp-server",
    "packages/libs",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
]
